# Copy this file to ".env" and replace the placeholders with local values.
# Do NOT commit your real .env file.
DATABASE_URL="postgresql://YOUR_USER:YOUR_PASSWORD@localhost:5432/YOUR_DATABASE?schema=public"
PORT=3000
# Optional comma-separated browser origins allowed to send credentialed requests.
# CLIENT_ORIGINS="http://localhost:5173,http://127.0.0.1:5173"
# Optional test-only initial password for Lab 3 seed users. Leave unset to keep
# the migration placeholder, or set it only in your private server/.env.
# LAB3_SEED_INITIAL_PASSWORD=
